"use client";import { logger } from "@/lib/logger";import { createContext, useContext, ReactNode, useEffect, useState } from "react";import { onAuthStateChanged } from "firebase/auth";import { doc, getDoc } from "firebase/firestore";import { useRouter, usePathname } from "next/navigation";import { auth, db } from "@/lib/firebase";
import { ensureUserProfile } from "@/lib/firestore";export interface UserProfile {  uid: string;  fullName: string;  username: string;  email: string | null;  photoURL?: string | null;}interface UserContextType {  user: UserProfile | null;  loading: boolean;}const UserContext = createContext<UserContextType>({ user: null, loading: true });/** Routes viewable without an account (no redirect / no skeleton). */function isPublicPath(pathname: string): boolean {  return (    pathname === "/" ||    pathname === "/ms" ||    pathname === "/login" ||    pathname === "/onboarding" ||    pathname.startsWith("/alert") ||    pathname.startsWith("/guardian/invite")  );}export function UserProvider({ children }: { children: ReactNode }) {  const [user, setUser] = useState<UserProfile | null>(null);  const [loading, setLoading] = useState(true);  const router = useRouter();  const pathname = usePathname();  useEffect(() => {    const unsubscribe = onAuthStateChanged(auth, async (firebaseUser) => {      if (firebaseUser) {        // Render immediately from what auth knows, then enrich from Firestore.        setUser({          uid: firebaseUser.uid,          fullName: firebaseUser.displayName || "User",          username: "user",          email: firebaseUser.email,          photoURL: firebaseUser.photoURL || null,        });        setLoading(false);        try {          const userDocRef = doc(db, "users", firebaseUser.uid);          const userDoc = await getDoc(userDocRef);          if (userDoc.exists()) {            const data = userDoc.data();            setUser({              uid: firebaseUser.uid,              fullName: data.fullName || firebaseUser.displayName || "User",              username: data.username || "user",              email: firebaseUser.email,              photoURL: data.photoURL || firebaseUser.photoURL || null,            });          }        } catch (error) {          logger.error("Error fetching user profile:", error);        }      } else {        setUser(null);        if (!isPublicPath(pathname)) {          let onboarded = false;          try {            onboarded = localStorage.getItem("guidr_onboarded") === "1";          } catch {            /* storage disabled â€” treat as not yet onboarded */          }          router.push(onboarded ? "/login" : "/onboarding");        }        setLoading(false);      }    });    return () => unsubscribe();  }, [pathname, router]);  // Loading skeleton that mimics the app chrome on protected routes.  if (loading && !isPublicPath(pathname)) {    return (      <div className="min-h-dvh bg-guidr-bg flex flex-col">        <div className="sticky top-0 flex items-center justify-between px-5 pt-safe-top pb-3 bg-white/90 border-b border-gray-100">          <div className="w-10 h-10 rounded-xl animate-pulse bg-gray-200" />          <div className="h-6 w-24 rounded animate-pulse bg-gray-200" />          <div className="w-10 h-10 rounded-full animate-pulse bg-gray-200" />        </div>        <div className="flex-1 px-5 py-4 flex flex-col gap-3">          <div className="h-32 rounded-2xl animate-pulse bg-gray-200" />          <div className="h-20 rounded-2xl animate-pulse bg-gray-200" />          <div className="h-20 rounded-2xl animate-pulse bg-gray-200" />          <div className="h-20 rounded-2xl animate-pulse bg-gray-200" />        </div>        <div className="lg:hidden h-16 border-t border-gray-100 bg-white flex items-center justify-around px-5 pb-safe-bottom">          {[0, 1, 2, 3, 4].map((i) => (            <div key={i} className="flex flex-col items-center gap-1">              <div className="w-6 h-6 rounded animate-pulse bg-gray-200" />              <div className="w-10 h-2 rounded animate-pulse bg-gray-200" />            </div>          ))}        </div>      </div>    );  }  return (    <UserContext.Provider value={{ user, loading }}>      {children}    </UserContext.Provider>  );}export function useUser() {  const ctx = useContext(UserContext);  if (!ctx) throw new Error("useUser must be used within UserProvider");  return ctx;}