import { Clock3, Sparkles, UsersRound } from "lucide-react";
export default function ScanQueueGame({ position }: { position: number }) { return <section className="scan-panel scan-panel-elevated guidr-animate-in overflow-hidden"><div className="bg-guidr-primary px-6 py-7 text-white"><div className="flex size-11 items-center justify-center rounded-full bg-white/15"><Clock3 size={22} /></div><p className="mt-5 text-xs font-bold tracking-[0.13em] text-white/70">SECURE REVIEW QUEUE</p><h1 className="mt-2 text-2xl font-bold">We saved your place</h1><p className="mt-2 max-w-sm text-sm leading-6 text-white/80">Each message is checked carefully so the service stays reliable for everyone.</p></div><div className="p-6"><div className="flex items-center gap-4"><div className="flex size-12 items-center justify-center rounded-lg bg-guidr-primary/10 text-guidr-primary"><UsersRound size={23} /></div><div><p className="font-semibold">{position > 0 ? `${position} ${position === 1 ? "scan" : "scans"} ahead of you` : "Finding your review slot"}</p><p className="mt-0.5 text-sm text-guidr-muted">This updates automatically every few seconds.</p></div></div><div className="mt-6 h-2 overflow-hidden rounded-full bg-guidr-surface"><div className="h-full w-2/5 animate-pulse rounded-full bg-guidr-primary" /></div><div className="mt-5 flex items-center gap-2 text-sm text-guidr-muted"><Sparkles size={16} className="text-guidr-primary" />Keep this page open while your place in the queue is reserved.</div></div></section>; }